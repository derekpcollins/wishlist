{
	"version": 2,
	"builds": [
		{ "src": "api/save.js", "use": "@vercel/node" }
	],
	"routes": [
		{ "src": "/api/save", "dest": "/api/save.js" }
	],
	"functions": {
		"api/**/*.js": {
			"runtime": "nodejs20.x"
		}
	}
}